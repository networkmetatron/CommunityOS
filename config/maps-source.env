# CommunityOS Maps — remote PMTiles source for region extracts
# Update this file (or PMTILES_SOURCE_URL in /opt/communityos/.env) when
# Protomaps publishes a newer daily build. No Python changes required.
#
# Format: https://build.protomaps.com/YYYYMMDD.pmtiles
# Validated CommunityOS default (2026-08-07 Protomaps build):
PMTILES_SOURCE_URL=https://build.protomaps.com/20260807.pmtiles
